; JAE TRMS — custom NSIS installer script
; Included by electron-builder via package.json > build.nsis.include.
; No custom macros are defined yet, so electron-builder's defaults apply.
; Add custom install/uninstall behavior here when needed, e.g.:
;
;   !macro customInstall
;     ; runs during installation
;   !macroend
;
;   !macro customUnInstall
;     ; runs during uninstallation
;   !macroend
